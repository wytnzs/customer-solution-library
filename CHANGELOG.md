# Changelog

## v3.1（2026-09）

- **客户标识改为真实姓名**：档案文件名、事件流目录、分析与洞察输出目录都用真实姓名；`customer_id` 保留为内部稳定键，仅用于合并、引用和对账。
- `capture` 新建草稿不再做脱敏拼接，直接用描述里的真实姓名/称呼；同名档案自动加 `-2`、`-3` 后缀。
- 命令 `--customer-id` 可直接传真实姓名（仍兼容编号）；人读输出以姓名为主、编号为辅。
- 隐私边界不变：真实客户数据只存在私有客户库；公开仓库、知识库与对外分享仍必须脱敏。
- 回归测试 104 → 108 项。

## v3（2026-08）

第八轮迭代完成，形成「录入 → 分析 → 筛选 → 生命周期」全链路：15 个命令，104 项回归测试全部通过。

系统不是一次建成的。每轮先想清楚补哪个业务缺口，再实现脚本 + 回归测试 + 同步文档。

| 轮次 | 解决的业务缺口 | 新增能力 | 回归测试 |
|---|---|---|---|
| 第 1 轮 | 脚本与文档从 0 到 1 | init/index/find/analyze/gaps/product-match/dashboard/audit/score-intake 9 个命令；修复 frontmatter 内联列表、误判、逾期跟进、find 排除产物、`--json` | 手工跑通 9 命令 |
| 第 2 轮 | find 排序不准、入口积压靠人工 | `find` 相关度排序；新增 `intake-pool` 批量评分；建立回归测试框架 | 20 项 |
| 第 3 轮 | 录入要一段段手动整理 | 新增 `capture` 抓取入档闭环、`segment` 群体分析、`product-match --detail` 名单→详情 | 39 项 |
| 第 4 轮 | 客户信息散、无法聚合画像 | 画像字段模型（年龄/收入/职业/城市/家庭结构）、capture 可补充项引导与疑似命中提示、segment 主要客户画像 + 保险类型 × 画像 | 56 项 |
| 第 5 轮 | 产品匹配靠人工判断 | 产品适配画像字段 + `product-match` 软信号画像评分，「客户群体 → 产品」反推闭环 | 61 项 |
| 第 6 轮 | 想按画像/保险类型筛客户 | 新增 `filter` 命令：画像/生命周期/保险类型/缺省字段四类条件，`--missing` 补全排期 | 69 项 |
| 第 7 轮 | 想知道客户群体集中在哪 | `segment --by` 单维度钻取，逐桶看客户与典型画像 | 78 项 |
| 第 8 轮 | 成交后、不回复的客户没有出口 | 新增 `set-status`（归档/唤醒/沉睡）+ `merge`（合并同一人）；`filter --status` 单独成条件；客户库骨架模板与数据边界说明 | 104 项 |

## 设计原则

- **稳定层 / 灵活层分开**：脚本做「对就是对」的事（确定性、可回归），AI 做「需要理解」的事（语义、判断）。
- **破坏性操作预览先行**：`capture`、`set-status`、`merge` 不带 `--apply` 只打印预览，确认后才写盘，改动写入档案变更记录。
- **真实数据隔离**：本仓库只保存规则、脚本与模板；真实客户资料放在单独的私有客户库，不提交到本仓库。
